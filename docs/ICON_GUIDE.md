# 应用图标设置指南

## 图标类型说明

QuickNote 需要两种图标：

1. **应用图标（App Icon）**：显示在 Finder、应用程序文件夹中的图标
2. **托盘图标（Tray Icon）**：显示在菜单栏的小图标（代码已生成）

## 创建应用图标

### 方式一：使用 Tauri 图标生成器（推荐）

1. **准备一张 1024x1024 的 PNG 图片**
   - 建议使用简洁的设计
   - 背景可以是透明或纯色
   - 推荐使用 📝 笔记本图标或字母 "N"

2. **使用 Tauri CLI 生成所有尺寸**

```bash
# 安装 Tauri CLI（如果还没安装）
cargo install tauri-cli

# 生成图标（会自动生成所有需要的尺寸）
cargo tauri icon path/to/your/icon.png
```

这会在 `src-tauri/icons/` 目录生成：
- `32x32.png`
- `128x128.png`
- `128x128@2x.png`
- `icon.icns` (macOS)
- `icon.ico` (Windows)

### 方式二：在线生成

1. 访问 https://icon.kitchen/
2. 上传你的图标或使用内置图标
3. 选择 "macOS" 平台
4. 下载生成的图标包
5. 将文件放到 `src-tauri/icons/` 目录

### 方式三：使用设计工具

使用 Figma、Sketch 或 Photoshop 创建以下尺寸：

- `32x32.png` - 小图标
- `128x128.png` - 标准图标
- `128x128@2x.png` - Retina 显示器图标（256x256）
- `icon.icns` - macOS 图标集（包含多个尺寸）

## 快速开始：使用 Emoji 图标

如果你想快速测试，可以用 emoji 创建图标：

```bash
# 创建一个简单的图标生成脚本
cat > create_icon.sh << 'EOF'
#!/bin/bash

# 使用 ImageMagick 创建图标
# 安装: brew install imagemagick

# 创建 1024x1024 的图标
convert -size 1024x1024 xc:none \
  -gravity center \
  -pointsize 800 \
  -font "Apple-Color-Emoji" \
  -annotate +0+0 "📝" \
  icon-1024.png

# 使用 Tauri 生成所有尺寸
cargo tauri icon icon-1024.png

echo "✅ 图标生成完成！"
EOF

chmod +x create_icon.sh
./create_icon.sh
```

## 推荐的图标设计

### 设计建议

1. **简洁明了**：图标应该在小尺寸下也清晰可辨
2. **品牌一致**：与应用的紫色渐变主题保持一致
3. **圆角设计**：macOS 风格通常使用圆角矩形
4. **高对比度**：确保在浅色和深色背景下都清晰

### 配色方案

使用应用的主题色：
- 主色：`#667eea` (紫色)
- 辅色：`#764ba2` (深紫色)
- 渐变：`linear-gradient(135deg, #667eea 0%, #764ba2 100%)`

### 图标元素建议

- 📝 笔记本 emoji
- ✏️ 铅笔图标
- 📄 文档图标
- 字母 "Q" 或 "N"
- 简化的笔记本线条图

## 配置图标

图标文件准备好后，确保 `src-tauri/tauri.conf.json` 中配置正确：

```json
{
  "bundle": {
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ]
  }
}
```

## 验证图标

构建应用后检查图标：

```bash
# 构建应用
cargo tauri build

# 查看应用图标
open src-tauri/target/release/bundle/macos/QuickNote.app
```

在 Finder 中应该能看到你的自定义图标。

## 常见问题

### Q: 图标显示为默认图标？
A: 确保图标文件格式正确（RGBA PNG），并重新构建应用。

### Q: 图标模糊？
A: 确保提供了 @2x 版本（Retina 显示器需要）。

### Q: 如何创建 .icns 文件？
A: 使用 `cargo tauri icon` 命令会自动生成，或使用 macOS 的 `iconutil` 工具。

## 在线资源

- [Icon Kitchen](https://icon.kitchen/) - 在线图标生成器
- [Tauri Icon 文档](https://tauri.app/v1/guides/features/icons/)
- [macOS 图标设计指南](https://developer.apple.com/design/human-interface-guidelines/app-icons)
- [免费图标资源](https://www.flaticon.com/)
